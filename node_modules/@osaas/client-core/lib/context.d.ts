export type ContextConfig = {
    personalAccessToken?: string;
    environment?: string;
};
export type ServiceAccessToken = {
    serviceId: string;
    token: string;
    expiry: number;
};
export type Service = {
    serviceId: string;
    apiUrl: string;
    serviceType: 'instance' | 'job';
};
export type Subscriptions = {
    teamId: string;
    services: string[];
};
export declare class Context {
    private personalAccessToken?;
    private environment;
    constructor(config?: ContextConfig);
    getPersonalAccessToken(): string | undefined;
    getEnvironment(): string;
    getServiceAccessToken(serviceId: string): Promise<string>;
    activateService(serviceId: string): Promise<void>;
    refreshServiceAccessToken(serviceId: string): Promise<string>;
}
//# sourceMappingURL=context.d.ts.map